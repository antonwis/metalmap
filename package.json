{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@vue-leaflet/vue-leaflet": "^0.8.2",
    "leaflet": "^1.9.3",
    "nuxt": "^3.1.2"
  },
  "dependencies": {
    "mariadb": "^3.0.2"
  }
}
